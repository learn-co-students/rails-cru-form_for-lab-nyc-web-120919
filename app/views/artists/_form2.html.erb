<h1>Edit Artist Form</h3>

<%= form_for(@artist) do |f| %>
    <label>Name:</label><br>
    <%= f.text_field :name %><br>
    <label>Bio:</label><br>
    <%= f.text_area :bio %>
    <%= f.submit "Update Artist" %>
    <% end %> 